# Shop API - Бэкенд для онлайн магазина

## 📋 Обзор проекта

Shop — это современный бэкенд API для электронной коммерции, созданный на базе фреймворка **FastAPI**. Система предоставляет полный набор функций для управления интернет-магазином, включая аутентификацию пользователей, каталог товаров, обработку заказов, систему отзывов и управление профилями.

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.12+
- Docker & Docker Compose (опционально, для контейнеризованного развертывания)
- PostgreSQL (или SQLite для разработки)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/VelichkinPetr/Shop.git
cd Shop
```

2. **Создайте виртуальное окружение и установите зависимости**
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

3. **Инициализируйте базу данных**
```bash
alembic upgrade head
```

6. **Запустите приложение**
```bash
fastapi dev main.py
```

API будет доступен по адресу `http://localhost:8000` с автоматической документацией по адресу `http://localhost:8000/docs`.

### Развертывание с Docker
```bash
# Сборка и запуск через Docker Compose
docker-compose build

# Запуск в фоновом режиме
docker-compose up --detach
```

## 📁 Структура проекта

```
Shop/
├── api/                    # Конечные точки API и роутеры
│   └── router/
│       └── v1/            # API версии 1
│           ├── order/     # Эндпоинты управления заказами
│           └── ...        # Другие модули эндпоинтов
├── core/                   # Основная логика приложения
│   └── security/          # Аутентификация и безопасность
├── database/               # Конфигурация базы данных
│   └── migrations/        # Файлы миграций Alembic
├── models/                 # Модели базы данных SQLAlchemy
├── schemas/                # Схемы Pydantic для валидации данных
│   └── reviews/           # Схемы для отзывов
├── services/               # Сервисы бизнес-логики
│   └── order/             # Сервис обработки заказов
├── alembic.ini            # Конфигурация Alembic
├── docker-compose.yml     # Конфигурация Docker Compose
├── Dockerfile             # Конфигурация Docker-образа
├── main.py               # Точка входа в приложение
├── requirements.txt      # Зависимости Python
└── test_database.db     # Тестовая база данных SQLite
```

## 🔧 Возможности

- **RESTful API** на фреймворке FastAPI
- **Миграции базы данных** с использованием Alembic
- **Поддержка Docker** для простого развертывания
- **Документация API** с Swagger UI и ReDoc
- **Модульная архитектура** с четким разделением ответственности

### Модули API
- **Управление пользователями** — регистрация, аутентификация, управление профилями
- **Каталог товаров** — списки товаров, категории, управление запасами
- **Обработка заказов** — корзина покупок, оформление заказа, история заказов
- **Система отзывов** — рейтинги товаров и обратная связь от покупателей
- **Административные функции** — возможности управления магазином


## 📚 Документация API

После запуска приложения доступна интерактивная документация:

- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`


## 📞 Поддержка

По вопросам или для получения поддержки, пожалуйста, создайте issue в репозитории GitHub.

